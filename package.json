{
  "name": "gbjam5",
  "version": "0.0.1",
  "scripts": {
    "prestart": "./build.sh build",
    "start": "npm run localhost & npm run watch",
    "localhost": "browser-sync start --server dist --files dist",
    "watch": "./build.sh watch"
  },
  "devDependencies": {
    "babel-preset-es2015-rollup": "1.2.0",
    "browser-sync": "2.14.0",
    "nodemon": "1.10.0",
    "pug-cli": "1.0.0-alpha6",
    "rollup": "0.34.8",
    "rollup-plugin-babel": "2.6.1"
  }
}
